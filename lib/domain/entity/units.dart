enum AltitudeUnit { meter, foot }

enum SpeedUnit { mph, kmh }

enum TimeFormat { format24h, format12h }

enum DistanceUnit { km, mile }

enum DateDisplayFormat { us, international }

enum TemperatureUnit { celsius, fahrenheit }
